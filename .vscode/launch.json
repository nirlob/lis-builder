{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: LIS Builder",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/launcher.py",
            "console": "integratedTerminal",
            "justMyCode": true,
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}
